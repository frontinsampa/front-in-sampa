<section class="features">
  <div class="features-main">
    <div class="features-content">
      <h2 class="features-title">FRONTIN Sampa + <br />Code In The Dark Brasil</h2>
      <p>O tradicional e maior evento de Front-End do Brasil, FRONTIN Sampa, chega a sua 12ª edição e não estará sozinho! </p>

      <p>FRONTIN Sampa e Code In The Dark Brasil, esses dois eventos, que já são sucesso há tanto tempo, se uniram novamente!</p>

      <p>Vão acontecer no dia 29 de julho no nosso queridinho Teatro Gazeta, no coração da avenida Paulista.</p>


      <div class="button-wrapper">
        <a class="button" href="https://www.eventbrite.com.br/e/frontin-sampa-2023-code-in-the-dark-tickets-574922567877?aff=erelexpmlt" target="_blank">
            COMPRE SEU INGRESSO AGORA!
            </a>
      </div>
    </div>
    <div class="features-photos">
      <ul class="features-gallery">
        <li><img src="/photos/code_2023_2.jpeg" alt="Code In The Dark - 2023" /></li>
        <li><img src="/photos/fsp_2019.jpeg" alt="Frontinsampa 2019" /></li>
        <li><img src="/photos/fsp_2018.jpeg" alt="Frontinsampa 2018" /></li>
        <li><img src="/photos/code_2023_3.jpeg" alt="Code In The Dark - 2023" /></li>

      </ul>
    </div>
  </div>
</section>

<style lang="scss">
  .features {
    position: relative;
    background-color: var(--color-primary);
  }

  .features-main {
    max-width: var(--wrapper-medium);
    width: 100%;
    margin: 0 auto;
    padding: 10rem 0 7rem;
    display: flex;

    @media (max-width: 1200px) {
      padding: 8rem 3rem 6rem 3rem;
    }

    @media (max-width: 800px) {
      display: block;
    }

    @media (max-width: 600px) {
      padding: 5rem 3rem 4rem 3rem;
    }
  }

  .features-content {
    flex-basis: 50%;
    padding-right: 7rem;

    @media (max-width: 800px) {
      padding-right: 0;
    }

    p {
      color: var(--color-secondary);
    }
  }

   .features-title {
    color: var(--color-secondary);
    font-size: 4rem;
  }

  .features-photos {
    position: relative;
    flex: 1;
    margin-top: -5rem;

    @media (max-width: 800px) {
      margin-top: 5rem;
    }
  }

  .features-photos:before {
    --size: 40rem;
    content: '';
    width: var(--size);
    height: var(--size);
    position: absolute;
    background: var(--color-secondary);
    left: 45%;
    top: 45%;
    margin-top: calc(-1 * calc(var(--size) / 2));
    margin-left: calc(-1 * calc(var(--size) / 2));
    animation: morph 15s linear infinite alternate, spin 20s linear infinite;
    z-index: 1;
    will-change: border-radius, transform;
    transform-origin: 55% 55%;
    pointer-events: none;
    opacity: 0.2;

    @media (max-width: 1000px) {
      top: 30%;
    }

    @media (max-width: 800px) {
      top: 50%;
      left: 40%;
    }

    @media (max-width: 600px) {
      top: 50%;
      left: 42%;
      --size: 30rem;
    }
  }

  .features-title {
    margin-bottom: .4em;
    font-size: 4rem;

    @media (max-width: 600px) {
      font-size: 3rem;
    }
  }

  .features-gallery {
    position: relative;
    margin: 0;
    padding: 0;
    list-style: none;
    z-index: 1;
    display: flex;
    flex-wrap: wrap;
  }

  .features-gallery li {
    height: auto;
    flex-basis: 51%;
  }

  .features-gallery li:nth-child(even) {
    margin-top: 4rem;
    margin-left: -2rem;
    margin-bottom: 0rem;
  }

  .features-gallery li:nth-child(3) {
    z-index: 5;
  }

  .features-gallery li img {
    max-width: 150%;
    height: auto;
    border: 2px solid var(--color-secondary);
    box-shadow: 6px 6px var(--color-secondary);

  }

  .features-gallery li:nth-child(1) img {
      transform: rotate(349deg);
  }

   .features-gallery li:nth-child(2) img {
      transform: rotate(12deg);
  }

   .features-gallery li:nth-child(3) img {
      transform: rotate(6deg);
  }

   .features-gallery li:nth-child(4) img {
      transform: rotate(38deg);
  }

  .button-wrapper {
    padding-top: .4rem;
  }

  .button-wrapper .button {
    font-size: 1.6rem;
    color: var(--color-secondary);
    padding: .5em 1em;

    @media (max-width: 600px) {
      font-size: 1.4rem;
    }
  }

  @keyframes morph {
    0% {
      border-radius: 40% 60% 60% 40% / 70% 30% 70% 30%;
    }

    100% {
      border-radius: 40% 60%;
    }
  }

  @keyframes spin {
    100% {
      transform: rotate(1turn);
    }
  }
</style>
